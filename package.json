{
  "name": "generated-app",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node server/index.js",
    "test": "node --test test/*.test.js",
    "web": "npm --prefix web run dev"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "supertest": "^7.1.1"
  }
}
